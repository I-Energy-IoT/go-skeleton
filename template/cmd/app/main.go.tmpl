package main

import (
	"{{.Name}}/config"
	"{{.Name}}/internal"
	"{{.Name}}/pkg/logger"

	"go.uber.org/fx"
	"go.uber.org/fx/fxevent"
)

func main() {
	app := fx.New(
		config.Module,
		logger.Module,
		fx.WithLogger(func(log logger.Logger) fxevent.Logger {
			return log
		}),
		internal.Module,
	)

	app.Run()
}
